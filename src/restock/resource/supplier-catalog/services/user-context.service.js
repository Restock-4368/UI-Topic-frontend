export function getActiveRestaurantId() {
// En una implementación real esto se obtendría del store o sesión del usuario logueado
// Por ahora devolvemos 1 como fallback para compatibilidad con tu sistema actual
    return 2;
}